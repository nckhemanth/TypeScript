# AGENTS.md

This is a public TypeScript + Zod learning repo.

If the private `workspace` hub is available, read this first:

- `workspace/context/repos/TypeScript/CONTEXT.md`
- `workspace/context/repos/TypeScript/CHANGELOG.md`

Rules:

- Do not commit secrets, `.env` values, raw transcripts, or private notes.
- Keep explanations concise, beginner-friendly, and interview-useful.
- Prefer practical examples and runnable labs over large rewrites.
- When adding learning content, include intuition, a small code example, common failure modes, and a short interview answer where useful.
- Keep labs runnable with `npm run typecheck` from the repo root.
